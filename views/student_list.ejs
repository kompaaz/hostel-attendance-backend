 <div>
        <h1>Rendering Users</h1>
        <ul>
          <% users.forEach(user => { %>
          <li><%= user.name %></li>
          <li><%= user.roll %></li>
          <hr>
          <% }) %>
        </ul>
      </div>
